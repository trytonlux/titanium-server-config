---
- name: Template Systemd Unit
  template:
    src: "container.service.j2"
    dest: "~/.config/systemd/user/container-{{ container_id }}.service"
  notify: Restart Container

- name: Enable Systemd Unit
  systemd:
    name: "container-{{ container_id }}"
    daemon-reload: true
    enabled: true
    scope: user
