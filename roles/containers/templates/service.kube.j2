[Unit]
Description=A kubernetes yaml based service
Before=local-fs.target

[Kube]
Yaml=/storage/containers/kube/{{ item }}.yaml

[Service]
Restart=on-failure

[Install]
WantedBy=multi-user.target default.target
